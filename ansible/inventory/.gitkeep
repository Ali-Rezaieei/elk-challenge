# The real inventory (hosts.yml) is GENERATED by Terraform from state and is
# gitignored. This .gitkeep keeps the directory present in a fresh clone so
# `terraform apply` has somewhere to write it.
